{
  "name": "localflow-licensing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe checkout and offline license issuing for LocalFlow Pro",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "keypair": "node scripts/generate-keypair.mjs",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
