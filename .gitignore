.build/
build/
.swiftpm/
.DS_Store
*.swp
DerivedData/

# Stripe / licensing secrets — never commit
server/.signing-key.json
server/.dev.vars
server/node_modules/
.wrangler/

# Internal notes — business/legal detail that has no reason to be public
CLAUDE.md
